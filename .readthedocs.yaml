# Build from the docs/ directory with Sphinx
sphinx:
  configuration: biobb_ml/docs/source/conf.py

# Explicitly set the version of Python and its requirements
python:
  version: 3.7
  install:
    - requirements: biobb_ml/docs/requirements.txt