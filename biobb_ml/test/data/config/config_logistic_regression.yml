properties:
  c_parameter: 0.01
  independent_vars:
    columns:
    - mean area
    - mean compactness
  normalize_cm: false
  scale: true
  solver: liblinear
  target:
    column: benign
  test_size: 0.2
